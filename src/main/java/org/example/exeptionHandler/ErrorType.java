package org.example.exeptionHandler;

public class ErrorType {
    public enum Error{
        CONNECTION_ERROR,
        DATA_PROCESSING_ERROR,
        WRITING_ERROR,
        READING_ERROR,
        VALIDATION_ERROR
    }
}
